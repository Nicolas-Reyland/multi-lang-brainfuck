#include <stdlib.h>
#include <stdio.h>

int main(int argc, char ** argv)
{
    if (argc != 2) {
        fprintf(stderr, "Usage: interpret input-file\n");
        exit(EXIT_FAILURE);
    }
    
    //
}